<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 300" width="1200" height="300" role="img" aria-label="Egor Khaklin — Proof over trust. You should never have to take the code's word for it.">
  <title>Egor Khaklin — Proof over trust</title>
  <style>
    /* ===== shared beam timeline — hero beam, reveal clip, and divider glint all ride this ===== */
    @keyframes beamSweep {
      0%   { transform: translateX(-160px); opacity: 0; }
      3%   { opacity: 1; }
      42%  { opacity: 1; }
      45%  { transform: translateX(1360px); opacity: 0; }
      100% { transform: translateX(1360px); opacity: 0; }
    }
    .gsweep { animation: beamSweep 20s linear infinite; }

    /* the reveal edge: same timing as the beam, but skewed -12deg so it slants WITH the beam.
       skew is baked into the rect's own transform (NOT a wrapper group) — a group inside a
       clipPath breaks the clip in WebKit/Safari, which is why the letters stopped lighting up. */
    @keyframes revealSweep {
      0%   { transform: skewX(-12deg) translateX(-160px); }
      45%  { transform: skewX(-12deg) translateX(1360px); }
      100% { transform: skewX(-12deg) translateX(1360px); }
    }
    .revealsweep { animation: revealSweep 20s linear infinite; }
    /* cumulative lit word: revealed by the growing clip, fades out just before the loop */
    @keyframes litFade { 0%,98% { opacity:1; } 100% { opacity:0; } }
    .litword { animation: litFade 20s linear infinite; }
    /* glass finish fogs up at the start of each cycle, then the sweep clears it */
    @keyframes frostFade { 0% { opacity:0; } 4% { opacity:1; } 98% { opacity:1; } 100% { opacity:0; } }
    .frost { animation: frostFade 20s linear infinite; }
    /* VERIFIED appears only after the sweep finishes (45%), holds, fades before reset */
    @keyframes verifiedAppear { 0%,45% { opacity:0; } 48% { opacity:1; } 98% { opacity:1; } 100% { opacity:0; } }
    .verified { animation: verifiedAppear 20s linear infinite; }
    @keyframes breathe { 0%,100% { opacity:.5; } 50% { opacity:.95; } }
    .pulse { animation: breathe 7s ease-in-out infinite; }
    @media (prefers-reduced-motion: reduce) {
      .gsweep,.revealsweep,.frost,.litword,.verified,.pulse { animation:none; }
      .gsweep { opacity:0; }   /* hide the moving beam/glint; the frost clip stays cleared, word + verified shown */
    }
  </style>
  <defs>
    <radialGradient id="bg" cx="40%" cy="30%" r="100%">
      <stop offset="0%" stop-color="#0c1620"/><stop offset="44%" stop-color="#070d14"/><stop offset="100%" stop-color="#05070a"/>
    </radialGradient>
    <linearGradient id="paneFill" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#0e2731" stop-opacity=".40"/><stop offset="50%" stop-color="#0a1922" stop-opacity=".26"/><stop offset="100%" stop-color="#06121a" stop-opacity=".46"/>
    </linearGradient>
    <linearGradient id="sheen" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#ffffff" stop-opacity=".16"/><stop offset="14%" stop-color="#ffffff" stop-opacity=".03"/><stop offset="100%" stop-color="#ffffff" stop-opacity="0"/>
    </linearGradient>
    <linearGradient id="shaft" x1="0" y1="0" x2="1" y2="0">
      <stop offset="0%" stop-color="#00bcd4" stop-opacity="0"/><stop offset="38%" stop-color="#00bcd4" stop-opacity=".10"/>
      <stop offset="50%" stop-color="#cdf6ff" stop-opacity=".9"/><stop offset="52%" stop-color="#ffffff" stop-opacity=".98"/><stop offset="54%" stop-color="#5cc8ff" stop-opacity=".9"/>
      <stop offset="64%" stop-color="#00bcd4" stop-opacity=".10"/><stop offset="100%" stop-color="#00bcd4" stop-opacity="0"/>
    </linearGradient>
    <!-- spectral fringe: brightest at the right (where it meets the core), trailing violet to the left -->
    <linearGradient id="fringe" x1="0" y1="0" x2="1" y2="0">
      <stop offset="0%" stop-color="#a98bff" stop-opacity="0"/><stop offset="42%" stop-color="#a98bff" stop-opacity=".26"/>
      <stop offset="74%" stop-color="#5cc8ff" stop-opacity=".42"/><stop offset="93%" stop-color="#9af1ff" stop-opacity=".6"/><stop offset="100%" stop-color="#eafdff" stop-opacity=".5"/>
    </linearGradient>
    <linearGradient id="wm" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#eafbff"/><stop offset="55%" stop-color="#bfeefb"/><stop offset="100%" stop-color="#67c9ef"/>
    </linearGradient>
    <filter id="soft" x="-30%" y="-30%" width="160%" height="160%"><feGaussianBlur stdDeviation="6"/></filter>
    <filter id="frostBlur" x="-20%" y="-20%" width="140%" height="140%"><feGaussianBlur stdDeviation="2"/></filter>
    <filter id="textGlow" x="-30%" y="-60%" width="160%" height="220%"><feGaussianBlur stdDeviation="3.2" result="b"/><feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge></filter>
    <filter id="frostTex" x="0" y="0" width="100%" height="100%">
      <feTurbulence type="fractalNoise" baseFrequency="0.9 0.9" numOctaves="2" seed="7" result="n"/>
      <feColorMatrix in="n" type="matrix" values="0 0 0 0 0.78  0 0 0 0 0.93  0 0 0 0 1  0 0 0 0.11 0"/>
    </filter>
    <rect id="paneShape" x="40" y="40" width="1120" height="220" rx="22"/>
    <clipPath id="paneClip"><use href="#paneShape"/></clipPath>
    <!-- growing reveal: rect's right edge sits at the beam; the wrapping skewX(-12) makes the
         reveal edge SLANT exactly like the beam, so each part of each letter lights up right as
         the beam crosses it (not before). base translate = full reveal for static/reduced-motion. -->
    <clipPath id="revealClip"><rect class="revealsweep" x="-2200" y="0" width="2200" height="300" transform="skewX(-12) translate(1360,0)"/></clipPath>
    <!-- complement of revealClip: covers the UN-opened region (right of the beam) for the frost finish -->
    <clipPath id="frostClip"><rect class="revealsweep" x="0" y="0" width="2600" height="300" transform="skewX(-12) translate(1360,0)"/></clipPath>
    <g id="wordmark"><text x="600" y="150" text-anchor="middle" font-family="'Helvetica Neue', Arial, system-ui, sans-serif" font-size="86" font-weight="700" letter-spacing="14">KHAKLIN</text></g>
  </defs>

  <rect width="1200" height="300" fill="#05070a"/>
  <rect width="1200" height="300" fill="url(#bg)"/>

  <g clip-path="url(#paneClip)">
    <g opacity=".08" stroke="#5cc8ff" stroke-width="1">
      <path d="M40 100 H1160 M40 150 H1160 M40 200 H1160"/>
      <path d="M200 40 V260 M400 40 V260 M600 40 V260 M800 40 V260 M1000 40 V260"/>
    </g>
    <use href="#paneShape" fill="url(#paneFill)"/>

    <!-- dim base: unlit letters read faintly (seen through the frost) -->
    <use href="#wordmark" fill="url(#wm)" opacity=".28" filter="url(#frostBlur)"/>

    <!-- cumulative bright word, revealed up to the beam and held lit -->
    <g class="litword"><g clip-path="url(#revealClip)"><use href="#wordmark" fill="#eafdff" filter="url(#textGlow)"/></g></g>

    <!-- GLASS FINISH: frosted veil over the un-opened (right-of-beam) region; the sweep opens it as it passes -->
    <g class="frost" clip-path="url(#frostClip)">
      <use href="#paneShape" fill="#bfe6f0" opacity=".18"/>
      <use href="#paneShape" filter="url(#frostTex)" opacity=".9"/>
      <use href="#paneShape" fill="#dff3fa" opacity=".06"/>
    </g>

    <!-- the beam: ONE connected slanted streak (fringe behind, halo, crisp core on top), all skewed together -->
    <g class="gsweep" opacity="0">
      <g transform="skewX(-12)">
        <rect x="-130" y="40" width="130" height="220" fill="url(#fringe)"/>
        <rect x="-80"  y="40" width="160" height="220" fill="url(#shaft)" filter="url(#soft)" opacity=".55"/>
        <rect x="-6"   y="40" width="12"  height="220" fill="url(#shaft)"/>
      </g>
    </g>

    <use href="#paneShape" fill="url(#sheen)"/>
  </g>

  <use href="#paneShape" fill="none" stroke="#5cc8ff" stroke-opacity=".34" stroke-width="1.2"/>
  <use href="#paneShape" fill="none" stroke="#ffffff" stroke-opacity=".06" stroke-width="3"/>

  <circle class="pulse" cx="74" cy="74" r="3.4" fill="#00bcd4"/>
  <text x="90" y="79" font-family="ui-monospace,'SF Mono',Menlo,monospace" font-size="14" letter-spacing="2" fill="#9fb4bd">@EgorKhaklin</text>
  <!-- VERIFIED: appears after the sweep -->
  <g class="verified" transform="translate(1042,62)">
    <rect x="0" y="0" width="118" height="26" rx="13" fill="#00bcd4" fill-opacity=".10" stroke="#00bcd4" stroke-opacity=".34"/>
    <circle class="pulse" cx="17" cy="13" r="4" fill="#5cc8ff"/>
    <text x="32" y="17" font-family="ui-monospace,'SF Mono',Menlo,monospace" font-size="12" letter-spacing="1" fill="#bfeefb">VERIFIED ✓</text>
  </g>
  <text x="600" y="206" text-anchor="middle" font-family="'Helvetica Neue', Arial, system-ui, sans-serif" font-size="14.5" font-weight="500" letter-spacing="5.5" fill="#9fb4bd">PROOF OVER TRUST</text>
  <text x="600" y="232" text-anchor="middle" font-family="ui-monospace,'SF Mono',Menlo,monospace" font-size="11.5" letter-spacing="1.5" fill="#5f7682">you should never have to take the code&apos;s word for it</text>
</svg>